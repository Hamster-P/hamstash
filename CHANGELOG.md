# Changelog
## [未发布]
- 修复部分条目一直显示「暂无简介」的问题：早期某次抓取失败会把占位串写进缓存且再不刷新，现在检测到这类坏行会自动重新抓取，不会再拿空简介覆盖已有的真简介
- 自动更新改用「实际生效代理」：开了系统代理开关（Clash 等）但没在设置页手填代理时，「检查更新」也能走上代理，不再直连 GitHub 失败
- 修复系统里设了 `HTTP_PROXY` 等代理环境变量时，本地 qBittorrent 连接被误发去代理导致连不上的问题
- 背景图/动漫logo 的语言优先级统一为 日文 > 中文 > 未定义语言 > 英文（未定义语言即 TMDB 里无字幕的纯画面 / 纯图形，动画类条目常见）
- 「未看集数」角标多处修复：① 此前「已看数」不分目录统计，看过总集篇 / SP / 花絮会把角标压成 0，现在只按正片目录、同一口径算；② RSS 追更下了新一集，整理入库时当场重算，不必手动「刷新 & 扫盘」；③ 看完一集角标立即 −1，重开软件也保持（此前要进一次该番详情页才更新）
- 支持**一个种子里同时含多季**的合集包（VCB「S1+S2」大合集这种）：整理时按每个文件名自己带的季度标记分别归入 Season 01 / Season 02，不再全部塞进一个季度、互相撞车导致正片整理失败
- 改名识别修正：`[Preview01]…` 下集预告片不再被当成正片改成 SxxExx；合集包里 `[SP01]…[SPNN]` 这类带方括号+序号的特典短片归到 Other，不再堆进 OVA
- 改名分类大修：
  - 修复**正片被误判成剧场版 / OVA**：此前关键词是朴素子串匹配，`SPY x FAMILY`、`Sports`、`Spice and Wolf` 会命中 `sp`，字幕组名 `[Movie-Fan]` 和剧名 `The Movie Star` 会命中 `movie`，`[85.5 MB]` 文件大小会命中 `.5`。现在一律带词边界，字幕组名不参与内容类型判定；Bangumi 官方标注为 TV 的条目也不再被猜成剧场版
  - 补上 `电影版 / 電影版` 的剧场版识别（此前只认 `剧场版 / 劇場版 / movie`）
  - 修复**中文集数被算错**：`第05话` 此前没有对应规则，会从「★4月新番」里抓个 `4` 出来当集数，静默产出一个看起来合理的错误集数。现在支持 `第X话/集`、`[02话]`、`【22】`，裸数字兜底也不再吃发布年份、日期和文件大小
  - 修复**中文季号只认个位**：`第十二季` 此前会退回 Season 01，现在支持到「十/百」进位和「两」
  - 修复**合集包里没有集数的文件整理失败**：种子标题写 `[01-12]` 时，`OVA.mkv`、`特典映像.mkv` 这类文件会全部被当成第 01 集、撞成同一个目标文件名（撞车本身有安全网兜住不丢文件，但这些文件会卡在暂存区进不了媒体库）。现在区间不再当作单集回退值，并按原文件名区分命名
  - **半集（12.5 话）改为跟正片同季**：此前被归到 `OVA/` 目录，现在留在 `Season NN` 里命名成 `S01E12.5`（小数保留，否则会跟同季第 12 集撞名互相覆盖；个位半集补零成 `S01E09.5`）。跨季连续编号的半集（如第三季发的 48.5）也能正确换算成季内编号
  - 修复**合集包里 `01.mkv`、`02.mkv` 这类裸编号文件名解析不出集数**的问题
  - 修复**「修复媒体库」对合集包文件反复提议改名、永不收敛**的问题：文件名里的 `[字幕组][分辨率]` 来自种子标题，而合集包内的文件名很裸（`01.mkv`），重算时拿不到这些信息就会算出比落地结果少一截后缀的名字。现在整理时会把种子标题一并存下来，修复重算时复现同一份输入（升级前已入库的文件没有这份记录，仍按旧行为处理）
  - 新增改名模块的回归测试（124 条真实种子标题语料 + negative case + 媒体库修复重算模拟），此前该模块零测试
- 「修复媒体库」增强：能收拾上面这类整理错乱的合集包——把误判的预告片 / 特典挪回 Other，把还卡在种子原始子目录里、整理失败的正片提回正确的 Season 目录（含跨季拆分），逐条确认后一键搞定

## [0.8.0] - 2026-09-03
### 影视库
- 媒体库详情页和剧场版页面，增加背景图，动漫logo，优化字体颜色和按钮图标
- 新增未看集数角标（可以在设置界面关闭），仅统计tv版和剧场版ova，不统计other分类的集数。
- 详情页新增「Bangumi介绍」按钮
- 「刷新 & 扫盘」按钮改为跟随「管理」模式显示/隐藏（点「管理」才出现在其旁边）
- 详情页和剧场版页的简介改为限高可滚动，低分辨率下不再被截断吞掉内容
- 修复剧场版页窗口较小时滚动卡片会露到顶部介绍区后面的问题
- 详情页/剧场版页头部去掉「播放渠道（电视台）」一行，太占空间
- 「重新匹配」绑定到新条目后，封面/背景图/LOGO 立即按新条目刷新，不用再手动「选择图片 → 恢复默认」
- 管理模式「选择图片」换封面时，简介同步换成所选那一部，封面与简介保持一致
- 搜索页画面微调优化
- 背景图/动漫logo 增加解析逻辑版本化：挑图规则升级后，老条目会在后台按新逻辑重新取图并替换缓存，重取期间先照旧显示上一版的图，不闪空
- 动漫logo 挑选规则调整：横向logo 优先（避免竖排标题挤在头部），同为横向时再按语言选；旧条目会自动按新规则重取
- 详情页/剧场版页头部的动漫logo 加重投影，缓解 logo 颜色与背景剧照相近时看不清的问题


### 补番
- 补番一览增加缓存优化性能
- 修复**从补番一览点进某部番、详情页「返回」却回到媒体库网格**的问题

### 设置画面
- 设置画面排版优化
- 「软件更新」里的「当前版本」改为按钮，点击弹窗展示 CHANGELOG 里全部历史版本的更新说明
- 「默认首页」新增「剧场版页面」选项，软件启动可直接进入剧场版画面

## [0.7.4] - 2026-08-31

### 软件更新
- 修复跳级升级时（比如落后好几个版本）设置页只显示最新一个版本更新说明的问题：现在会拼接展示当前版本到目标版本之间的全部更新内容
- 更新说明展示区域改为限高可滚动，避免多版本内容拼接后把设置页其他区块顶下去

### 季度编号
- 修复**同一部番的相邻两集分处两个季度目录**的问题：季度编号规则更新后（0.7.3 修好了「不同演绎」误伤），已经下载好的旧文件不会自动重排，于是升级前后下载的集数会落进不同的 Season 目录。「修复媒体库」此前也检测不出来——它只校验「这个季号存不存在」，而不校验「这个文件是不是真属于这一季」
- 「修复媒体库」现在会反查每个文件**当初是按哪个条目下载的**，据此校正季号，并给出改名建议（照旧逐条确认后才动文件）。反查不到来源的文件（手动拖进库的、单次下载的）保持原样，不猜
- 季度编号规则有更新时，设置页的「修复媒体库」会给出一次性提示，扫描过就自动消失

### AnimeGarden 字幕组识别
- 修复 AnimeGarden 搜索结果里**大量资源显示成「未知字幕组」**的问题：上游只对它自己登记过的字幕组填 `fansub` 字段，个人发布或未登记的组（如「晚街与灯」）组名只出现在 `publisher` 里。现在按 `fansub` → `publisher` → 标题解析依次兜底，跟 RSS 那条路径的判定口径拉齐
- 修复**按未登记字幕组搜索/订阅时一条结果都没有**的问题：上游收到不认识的字幕组名会直接返回空，而不是忽略该条件。现在查空会自动退回不带字幕组条件重查一次，交给本地过滤——已登记的字幕组不受影响，不会多发请求
- **升级时会自动把 AnimeGarden 上「未知字幕组」的订阅重置为「不限」**，并在 RSS 订阅页给出一次性提示。这些订阅原本靠「未知字幕组」这个临时标识匹配，标识不再产生后会静默失效、再也抓不到新集数。只影响 AnimeGarden，dmhy 的同名标识语义不同，不受影响



### 其他修复
- 修复**字幕组 v2 修正版先于原始版下载完成时，晚到的 v1 污染媒体库**的问题
- 修复 AnimeGarden 资源**大小虚报 1024 倍**的问题

## [0.7.3] - 2026-08-20

### 系列归属可以自己定了
- 下载页在「RSS订阅」和「自动改名」之间新增**「合并到主系列」**开关：关掉之后这一部独立成一部，在媒体库单独一张卡、单独一个文件夹，不并进系列的家族目录
- 媒体库详情页管理模式下，每个分季/剧场版/OVA 分组都新增**「调整归属…」**：可以把误合并的内容**拆成单独一部**，也可以**合并到其他系列**；单集剧场版同样支持
- 判断不出属于哪一部时（比如高达家族的 OVA 桶塞着十几部），弹窗列出整个家族让你自己挑，不再是"系统认不出就没有按钮"
- **调整归属会真的移动文件**，并同步改名记录、观看进度、剧场版列表、媒体库绑定；搬完还会自动按新归属把文件名重排好，**不需要再手动跑一次「修复媒体库」**
- 你的手动决定单独存表，不会被「修复媒体库」的缓存清理抹掉

### 季度编号修正
- 修复**同一季拆成两段播出时，每段各占一个季号**的问题：Re:Zero 第三季（袭击篇/反击篇）、第四季（丧失篇/夺还篇）此前被编成第三～第六季，现在正确合并为第三季、第四季
- 修复**重制版/剪辑版抢占季号**的问题：机动战士Z高达此前拿不到季号（被自己 2005 年的剧场版剪辑版挤掉，掉进 Season 00 被播放器当特典），ZZ 顶替成第二季；现在是 01 初代 / 02 Z高达 / 03 ZZ。高达SEED 的两部 HD重制版也不再占用第三、第四季
- **算不出季号的旁支不再堆进 `Season 00`**，改为按作品名单独建目录（`雷霆宙域`、`独角兽 RE_0096`、`0083 星尘的回忆`…），目录名自动去掉重复的系列前缀。这些目录现在也能在详情页正常显示、被「修复媒体库」正常处理
- **升级后会自动重算家族关系**：季度算法改动过去对已缓存的系列不生效（改好了也看不到变化），现在启动时自动检测并重算，不需要手动清理任何东西

### 改名与修复
- 改名预览此前不换算跨季连续编号，**预览显示的集数和下载后实际落地的不一致**（字幕组发的 `- 78` 预览成 E78，实际是第四季第 12 集），现已一致
- 修复「修复媒体库」把**剧场版正确的文件名改错**的问题（副标题被抹掉，`机动战士高达 闪光的哈萨维` 被改成 `机动战士高达`）
- 修复对**已拆分出来的文件夹**跑「修复媒体库」时，会把整个系列的归属根改成被拆出那一季的问题（会导致此后新下载的剧集全部落进错误的文件夹）
- 跨文件夹移动时观看进度不再丢失

### 下载与整理稳定性
- 大文件移动超时问题改善：qBittorrent 移动文件是异步的，此前等待逻辑在网络共享盘上容易误判失败
- 修复整理流程在任意一步中断后，下一轮轮询无法自动修复、种子永久卡住的多处问题
- 剧场版分类规则优化：同一系列内标题互为前缀的多部剧场版不再被归错

## [0.7.2]
### 性能改善与功能增强
- 家族树查询功能新增缓存，全面提升改名时候的查询性能
- 媒体库封面可自选：管理模式下每部番新增“选择图片”，可从该系列家族的全部作品里挑一张作封面
- 设置页新增“媒体库默认封面”策略：最新一季(TV) / 第一季 / 保持匹配条目本身；未手动选图的番按此自动选封面，纯剧场版用匹配条目本身的图（封面数据缓存优先，仅缺失才联网）
### 画面功能新增
- 新增“剧场版”画面，单独展示库内剧场版/OVA，每部一张卡（同一条目的多集归为一张卡），
- 后续下载剧场版/OVA 时自动加入；也可以把任意一集非tv版的内容手动设为独立剧场版/OVA
- 每张卡可“重选条目”换封面、“仅移出列表”（保留文件）或“删除文件”；封面/简介来自所选 Bangumi 条目，缓存优先
- 与原有模式互不影响：被设为独立的文件仍显示在原系列分集里，观看进度两边同步

## [0.7.1] - 2026-08-14
- 修复了修复数据库功能的回归 bug
- 修改为冷启动时显示等待提示而非误弹 qb 引导页
- 优化搜索页结果数量
- 当网络不稳定时候，自动改名取得文件夹根目录可能会发生错误。改成从缓存中获取路径，而不是每次都从bangumi获取。
- 暂时无法访问媒体时，不自动切换备用库，新增提示路径出现问题。并新增自动重连功能。
- RSS轮询时，如果无法访问代理，则新增显示提示。同时新增刷新全部RSS功能。

## [0.7.0] - 2026-08-10
### 下载源
- 下载源（dmhy / AnimeGarden / nyaa）从代码内置改为可在设置页管理：可停用某个源（从下载页下拉隐藏，已有订阅继续轮询），或在站点换域名/换镜像时修改地址
- 后端重构为统一的下载源适配器（SourceAdapter）注册表，新增数据源只需加一个适配器模块
- 搜索与 RSS 订阅统一走同一套过滤判定（后端权威过滤），保证「搜索页看到的」和「订阅实际下载的」一致；AnimeGarden 订阅改为与搜索同源取数
- 修复字幕语言筛选在 RSS 订阅侧对「简体/繁体」等实际不生效的问题（此前订阅与搜索的字幕判定不一致）
- 设置页新增下载源管理，至少需启用一个源；代理测试改为只探测当前启用的源（并使用其配置地址）

### 界面
- 下载详情页新增显示文件级别详情的功能
- 下载详情页新增分页功能，默认显示最新20条记录
- 搜索页和媒体库页面新增滚动条记忆功能

## [0.6.2] - 2026-08-09
- 修复搜索页漏掉部分日本剧场版/OVA（如「游戏王剧场版 光之金字塔」）的问题
- 影视库详情页返回时自动补全未加载的封面

## [0.6.1] - 2026-08-07
- 优化媒体库加载性能
- 修复国内代理没法更新的问题

## [0.6.0] - 2026-08-08
- 新增应用内自动更新：设置页可查看当前版本、手动检查更新，有新版时下载并安装（更新包托管在 GitHub Releases，安装需管理员授权，完成后自动重启）
- 修复「修复媒体库」改名后该集观看进度丢失的问题
- 导入插件tauri-plugin-window-state 记住主窗口的大小和位置

## [0.5.2] - 2026-08-06
- 新增修复数据库功能，应对自动改名抓取季度错误的情况，提供后续修复渠道
- 自动改名功能加强，增加剧场版判断，抓取到【M28】类似字段，判断为剧场版，进行剧场版处理

## [0.5.1] - 2026-08-03（首次公开发布）

### RSS 订阅
- RSS 订阅从"托管给 qBittorrent 自带的 RSS + 自动下载规则"改为完全自建的轮询引擎，解决原生方案在 nyaa 等需要二次请求 .torrent 文件的源上不可靠、规则重名冲突等问题
- 订阅一览新增"上次轮询时间"展示，删除前增加二次确认
- 轮询间隔可在设置页调整（1–1440 分钟）

### 下载
- 修复 AnimeGarden 站点搜索因单个 CJK 字符筛选词查不到结果的问题，字幕语言筛选改为完全前端本地过滤
- 新增 nyaa.si 数据源
- 补上视频格式对应关系，改名逻辑与字幕组识别多处修复
- 种子下载恢复功能：重启/异常退出后能继续跟踪之前提交的下载任务

### 追更 / 媒体库
- 追更日历增加缓存、只保留日本制作条目
- 媒体库支持删除条目、图片封面缓存机制
- 新增"补番"功能，翻页与排序逻辑修复

### 播放
- 新增 PotPlayer 外部播放器监视功能，通过窗口标题同步观看进度

### 稳定性 / 安装体验
- 新增 SQL 连接池，修复长时间休眠后数据库无法访问的问题
- 安装程序升级默认改为覆盖安装（保留已有数据）
- 新增设置主页、代理连通性测试

### 项目文档
- 补齐 GPL-3.0 协议、重写 README、新增详细使用手册（`docs/MANUAL.md`）
