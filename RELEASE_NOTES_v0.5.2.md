## 新增：修复媒体库

设置页新增「修复媒体库」入口，用来修正历史入库文件的命名/路径跟当前改名规则不一致的情况。

- **两阶段确认**：先点「扫描」列出所有发现的问题，逐条勾选确认后再点「应用修复」才会真正动文件，不会一键盲改
- **自动拦截风险项**：目标位置已存在别的文件、或同一批建议内部目标撞车的条目会被标出来并禁止勾选，避免覆盖丢数据
- **字幕跟随改名**：视频改名时，同目录下同名的字幕文件（.ass/.srt 等）一起跟着改
- **清理失效记录**：可选清理指向已不存在文件的整理记录、已不存在暂存目录的记录，以及媒体库文件夹索引（等同于影视库页的「刷新 & 扫描」）
- 每次扫描都会重新拉取季度关系数据，不会拿本地的过时缓存去改文件

## 改名逻辑加强

以下改动同时作用于「修复媒体库」和**以后新下载的种子**：

- **剧场版编号识别**：认得 `[M28]` 这类字幕组用来标记「剧场版第 N 部」的方括号数字编号（柯南、哆啦A梦这类长篇系列的常见命名），不再被误判成 TV 正片塞进 Season 目录
- **跨季绝对编号换算**：部分字幕组发布续季时沿用从第一季连续数下来的绝对集数（例：咒术回战第二季发布为 25–47 集）。现在会在集数超出该季自身总集数时，自动减去前序各季的累计集数，换算成季内编号（→ 01–23）。没超上限的一律原样保留，不做猜测
- **季度归属校正**：`Season NN` 文件夹的编号如果在当前季度关系表里已经不存在（早期版本按「第几个 cour」逐个编号留下的目录，比如实际只有 3 季却有 Season 05），会按 cour 位置反查出真实季度并给出改名建议
- **集数口径修正**：季度集数统一优先取 Bangumi 的 `eps` 字段、为空时才回退 `total_episodes`（后者会把特典/SP 算进去，偏大，会导致偏移量算错）

## 修复

- 修复「修复媒体库」应用改名后，数据库里记录的文件路径没有同步更新的问题（相对路径分隔符风格不一致导致匹配永远失败）
- 加固失效记录检测：避免 Linux/Docker 部署下因路径分隔符差异，把有效记录误判成失效并清除

## 文档

- 使用手册（`docs/MANUAL.md`）补充「修复媒体库」的使用说明
